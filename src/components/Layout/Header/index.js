import React from 'react';
import { HeaderContainer, HeaderTitle } from './styled';

const Header = () => (
  <HeaderContainer>
    <HeaderTitle>
      Soccer App
    </HeaderTitle>
  </HeaderContainer>
);


export default Header;
